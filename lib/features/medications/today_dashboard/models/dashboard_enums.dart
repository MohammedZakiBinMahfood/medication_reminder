enum DashboardSectionType { overdue, upcoming, completed }

enum DashboardFilterType {
  all,
  upcoming,
  completed,
  missed,
  critical,
  normal,
  low,
}

enum DashboardSortType { time, priority, name }

enum DashboardDoseAction { taken, skipped, snoozed }
